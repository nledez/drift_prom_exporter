[project]
name = "drift-prom-exporter"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "hvac>=2.4.0",
    "prometheus-client>=0.24.1",
    "pyopenssl>=25.3.0",
    "pyyaml>=6.0.3",
]

[dependency-groups]
dev = ["pytest>=8.0"]
